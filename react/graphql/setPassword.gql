mutation SetPassword($email: String!, $currentPassword: String!, $newPassword: String!) {
  setPassword(email: $email, currentPassword: $currentPassword, newPassword: $newPassword, vtexIdVersion:"myaccount-ui")
}